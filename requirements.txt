       python-telegram-bot>=20.0 
       firebase-admin>=6.0.0  # Si vous décidez de revenir à Firestore pour les données
       python-dotenv>=0.19.0
       Flask>=2.0.0
       gunicorn>=20.0.0
       Werkzeug>=2.0.0 # Souvent une dépendance de Flask/Gunicorn, bon à spécifier
       